import { useEffect } from 'react';
import { Box } from '@mui/material';

const Profile = () => {

  useEffect(() => {
  }, []);

  return (
    <Box textAlign="center">
      Profile
    </Box>
  );
};

export default Profile;

import { useEffect } from 'react';
import { Box } from '@mui/material';

const Configuration = () => {
  useEffect(() => {}, []);

  return <Box textAlign="center">Configuration</Box>;
};

export default Configuration;

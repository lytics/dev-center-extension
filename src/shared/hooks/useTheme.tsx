// Placeholder export to maintain file structure
export const useTheme = () => {
  throw new Error(
    'useTheme from this file is deprecated. Use import { useTheme } from "@mui/material/styles" instead.',
  );
};
